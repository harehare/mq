# HCL Implementation in mq

# Parses an HCL string and returns the corresponding data structure.
def hcl_parse(input):
  _hcl_parse(to_string(input))
end

# Serializes a value to an HCL string.
def hcl_stringify(data):
  _hcl_stringify(data)
end
